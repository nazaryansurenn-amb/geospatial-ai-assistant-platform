target-version = "py312"

[lint]
# Errors only: syntax errors, undefined names, invalid comparisons and misplaced
# statements. Formatting and style are left as written.
select = ["E9", "F63", "F7", "F82"]

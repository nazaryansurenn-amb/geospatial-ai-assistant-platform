# Plain-language agent responses on 8526

The owner reported an Armenian overview containing machine identifiers, English
class names and raw map configuration, plus an unsupported description of actual
2026 use as proven. This is a response-presentation correction only.

The separate v3 service supplies readable selected-area and section names to the
model, while keeping the numerical tool protocol private. User questions are
separate from trusted map context. Its wording contract uses the Armenian labels
visible in the five existing tabs and describes satellite-derived states as
observational estimates, not proven actual use.

Before delivery, a deterministic check detects machine identifiers, configuration
dumps and untranslated interface words. A detected problem triggers at most two
rewrites preserving facts and quantities. A persistently unsuitable reply returns
a localized error instead of exposing the rejected draft. No always-on extra model
call is added; all four primary live regression questions passed directly or with
their normal numerical tool call. Mixed-language replies are checked by the
language actually used as well as the requested language.

Armenian descriptions also retain the distinctions between annual use and cycle
count, missing versus ambiguous type observations, and terrain-based potential
candidates versus confirmed hydraulic feasibility. These clarify existing rules;
they introduce no analytical rule change.

Verified: the exact reported question, class descriptions, selected-area/potential
explanation, and Aknalich's preserved measured-active total of 354.1724 ha. Twelve
focused Python checks passed, including forced bad-draft correction and bounded
failure. Live desktop/mobile responses use readable Armenian and preserve the
existing layers bar. The frontend, analytical APIs, index and map assets are the
same preserved v2 files. 8525 remains unchanged.

New runtime: `run_agent_review_v3.py`, `wp_core/agent_service_v3.py`,
`wp_core/agent_knowledge_v3.py`, `wp_core/agent_presentation.py`.
Earlier v1/v2 runtimes and results remain sealed. Existing approval for the API
payload and GPT-5.4 mini remains in force; no older agent configuration is loaded.

Evidence: `server_data/agent_v1/language_verification_v3.json`,
`output/playwright/agent_language_v3/verification.json`, and
`server_data/agent_v1/http_verification.json`. Refreshing the page starts a fresh
conversation; already displayed older replies are not retroactively rewritten.
